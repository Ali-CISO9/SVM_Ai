export interface SystemStats {
    label: string;
    value: string;
    subLabel: string;
    icon: string;
    highlight?: boolean;
}

export interface PredictionResult {
    label: string;
    status: string;
}
